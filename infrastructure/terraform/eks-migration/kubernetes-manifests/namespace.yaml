# Kubernetes Namespace for Backend Application
# Creates an isolated namespace for backend resources
# Requirements: 1.2, 3.5

apiVersion: v1
kind: Namespace
metadata:
  name: backend
  labels:
    name: backend
    environment: exploration
    purpose: eks-migration-testing
